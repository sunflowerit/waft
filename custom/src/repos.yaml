---

odoo:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/OCB.git
    sunflowerit: https://github.com/sunflowerit/OCB.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

OCA/web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

